{#
/**
 * @file
 * Template pour la page de recherche de véhicules - Affichage des résultats
 * Display ID: page_1
 */
#}

{% if rows %}
  <div class="offresMoment">
    <h2><span>Résultats de votre recherche</span></h2>
    <div class="container">
      <div class="row">
        {% for row in rows %}
          <div class="itemOffre col-6 col-md-4 col-lg-3">
            <a href="{{ row.url }}">
              <div class="visuOffre">
                {% if row.marque_logo %}
                  <img class="iconMarque" src="{{ row.marque_logo }}" alt="Logo {{ row.marque_nom }}">
                {% endif %}
                {% if row.image %}
                  <img src="{{ row.image }}" alt="{{ row.title }}">
                {% endif %}
              </div>
              <p>
                <span class="typeVehicule">{{ row.title }}</span>
                à partir de
                <span class="prix">{{ row.prix }} DH TTC</span>
              </p>
            </a>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
{% else %}
  <div class="no-results">
    <div class="container">
      <div class="no-results-content">
        <h2>Aucun véhicule trouvé</h2>
        <p>Aucun véhicule ne correspond aux critères de recherche sélectionnés.</p>
        <p>Veuillez modifier vos filtres et essayer à nouveau.</p>
        <a href="/recherche-vehicule" class="btn btn-primary">Nouvelle recherche</a>
      </div>
    </div>
  </div>
{% endif %}
